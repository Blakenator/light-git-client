# Shorten the .pnpm virtual store directory names.
# Required for electron-builder on Windows — the default length (120)
# combined with deeply nested template paths (e.g. NSIS includes) can
# exceed the 260-char MAX_PATH limit that 32-bit tools like NSIS enforce.
virtual-store-dir-max-length=80
